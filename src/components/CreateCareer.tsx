import { useState,type FormEvent } from 'react';
import { Bike,ChevronRight } from 'lucide-react';
import { createRider } from '../data/riders';
import type { Rider,RiderProfile } from '../game/models';
const profiles:{id:RiderProfile;name:string;desc:string}[]=[{id:'grimpeur',name:'Grimpeur',desc:'Léger et résistant dans les grands cols.'},{id:'puncheur',name:'Puncheur',desc:'Explosif sur les reliefs courts.'},{id:'sprinteur',name:'Sprinteur',desc:'Puissant dans les arrivées rapides.'},{id:'rouleur',name:'Rouleur',desc:'Taillé pour les efforts longs.'},{id:'classiqueur',name:'Classiqueur',desc:'Solide sur les pavés et dans le vent.'},{id:'complet',name:'Complet',desc:'Équilibré sur tous les terrains.'}];
export function CreateCareer({onCreate}:{onCreate:(r:Rider)=>void}){const [profile,setProfile]=useState<RiderProfile>('complet'); const submit=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);onCreate(createRider({firstName:String(f.get('firstName')),lastName:String(f.get('lastName')),nationality:String(f.get('nationality')),age:Number(f.get('age')),height:Number(f.get('height')),weight:Number(f.get('weight')),profile}))}; return <main className="create"><div className="brand"><Bike/><span>CYCLING / CAREER</span></div><section className="intro"><span className="eyebrow">NOUVELLE CARRIÈRE · SAISON 2027</span><h1>Écrivez votre<br/><i>propre légende.</i></h1><p>Chaque choix façonne un coureur. Chaque course raconte une histoire.</p></section><form onSubmit={submit} className="creator"><div className="form-head"><b>01</b><div><h2>Identité du coureur</h2><p>Votre aventure commence ici.</p></div></div><div className="fields"><label>PRÉNOM<input name="firstName" required defaultValue="Julian"/></label><label>NOM<input name="lastName" required defaultValue="Morel"/></label><label>NATIONALITÉ<input name="nationality" required defaultValue="France"/></label><label>ÂGE<input name="age" type="number" min="18" max="25" defaultValue="19"/></label><label>TAILLE (CM)<input name="height" type="number" min="155" max="205" defaultValue="178"/></label><label>POIDS (KG)<input name="weight" type="number" min="48" max="100" defaultValue="66"/></label></div><div className="form-head profile-head"><b>02</b><div><h2>Profil sportif</h2><p>Choisissez vos forces naturelles.</p></div></div><div className="profiles">{profiles.map(p=><button type="button" className={profile===p.id?'selected':''} onClick={()=>setProfile(p.id)} key={p.id}><strong>{p.name}</strong><small>{p.desc}</small></button>)}</div><button className="primary big">COMMENCER LA CARRIÈRE <ChevronRight/></button></form></main>}
